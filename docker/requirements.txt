# Auto-Claude Docker Web UI Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Templates & Static Files
jinja2>=3.1.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# Data Validation
pydantic[email]>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
cryptography>=42.0.0
authlib>=1.3.0  # OIDC/OAuth2 support

# HTTP Client (for health checks, API calls)
httpx>=0.26.0

# WebSocket support (for real-time logs)
websockets>=12.0

# Async utilities
anyio>=4.0.0

# Markdown rendering (for spec display)
markdown>=3.5.0

# Email (SMTP)
aiosmtplib>=3.0.0
